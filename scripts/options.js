define(function () {
    // the lengths are specified relative to canvas width
    return {
        SPEED_MIN: 0.0000,
        SPEED_MAX: 0.0008,
        MARGIN: 0.07,
        LLOYD_WEIGHT: 0.1
    };
});